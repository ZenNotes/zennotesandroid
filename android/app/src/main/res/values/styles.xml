<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. Dark parent + explicit dark window chrome so
         the edge-to-edge margin areas (adjustMarginsForEdgeToEdge) never show
         a light background behind the dark WebView. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/znBackground</item>
        <item name="android:statusBarColor">@color/znBackground</item>
        <item name="android:navigationBarColor">@color/znBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/znBackground</item>
        <item name="android:statusBarColor">@color/znBackground</item>
        <item name="android:navigationBarColor">@color/znBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowBackground">@color/znBackground</item>
        <item name="android:statusBarColor">@color/znBackground</item>
        <item name="android:navigationBarColor">@color/znBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
